# Журнал прогресса проекта

Здесь фиксируется история выполненных задач: дата, что сделано, какие файлы затронуты, статус тестов, что осталось.

## 2026-07-23 — v0.3.5 Product Flavor Profile Registry (первый батч)

**Сделано:** создан независимый read-only модуль `src/lib/product-flavor-profile/` (registry числовых вкусовых характеристик `sweetness`/`sourness`/`freshness`/`richness` для `RESOLVED`-продуктов из Tobacco Identity Decision Registry). Заполнен первый приоритетный батч — 15 продуктов, отобранных по очереди `verifiedMixCount → componentOccurrenceCount → occurrenceCount` (убывание), с evidence (`MANUFACTURER_CLAIM` / `REVIEW_AGGREGATE` / `EDITORIAL_ASSESSMENT`) и confidence на каждое значение. Registry не подключён к scoring/UI — отдельная будущая итерация.

**Файлы:**
- создано: `src/lib/product-flavor-profile/{types,constants,errors,validation,registry,queries,public-mapper,index,product-flavor-profile.test}.ts`, `scripts/verify-product-flavor-profile.ts`, `docs/architecture/product-flavor-profile-registry.md`, `docs/engine-changelog/v0.3.5-product-flavor-profile-registry.md`;
- изменено: `package.json` (регистрация скрипта `verify:product-flavor-profile`, одна строка), `docs/roadmap/README.md` (перенос v0.3.5 в Completed).
- не изменено: Prisma schema/миграции, существующие engines, `TobaccoIdentityDecision`-записи.

**Тесты:** Vitest 933/933 (28 файлов, было 906/27, +27, все 27 — мои); `tsc --noEmit` — чисто; `eslint .` — чисто; `verify:product-flavor-profile` — 15 продуктов, 28 заполненных измерений, 55 evidence, confidence LOW/MEDIUM/HIGH = 6/9/0, HIGH не использован ни разу; `verify:tobacco-identity-decisions` и `verify:canonical-mix-scoring` пройдены без изменений. `verify:mix-profile`, `verify:mix-compatibility`, `verify:canonical-catalog-identity` не проходят в этой сессии из-за отсутствия локального PostgreSQL на `localhost:5432` — причина не связана с изменениями (эти модули не трогались).

**Осталось:** заполнить оставшиеся 71 из 86 `RESOLVED` продуктов (следующие батчи той же очереди); подключение registry к `calculateMixAnalysis`/scoring/UI — отдельная будущая итерация (не в этой сессии); локальный коммит в отдельной ветке/worktree ещё предстоит.

## 2026-07-23 — v0.3.5 Product Flavor Profile Registry (второй батч + tie-break для будущих батчей)

**Сделано:** заполнен второй приоритетный батч — ещё 15 продуктов (ранги 16–30 той же очереди), покрытие выросло с 15 до 30 из 86 `RESOLVED`. Данные batch 1/batch 2 вынесены в отдельные версионированные файлы `batch-1.ts`/`batch-2.ts` (по образцу `p0-decisions-batch-N.ts`), общие evidence-хелперы — в `evidence-helpers.ts`; `registry.ts` теперь только объединяет батчи и валидирует результат, публичный контракт `index.ts` не изменился. По отдельному запросу добавлен и явно закодирован вторичный tie-break очереди приоритизации для batch 3+ (manufacturer priority tier: Tier 1 — Darkside/Chabacco/MustHave/Sapphire Crown/Element, Tier 2 — Sebero/Overdose/Husky/Brusko/BlackBurn, Tier 3 — остальные), не меняющий и не пересчитывающий уже отобранные batch 1–2.

**Файлы:**
- создано: `src/lib/product-flavor-profile/{batch-1,batch-2,evidence-helpers,priority-queue,priority-queue.test}.ts`, `docs/engine-changelog/v0.3.5-product-flavor-profile-registry-batch-2.md`;
- изменено: `src/lib/product-flavor-profile/{registry,index,product-flavor-profile.test}.ts`, `scripts/verify-product-flavor-profile.ts` (порог 15 → 30), `docs/architecture/product-flavor-profile-registry.md`, `docs/roadmap/README.md` (30/86 вместо 15/86);
- не изменено: Prisma schema/миграции, существующие engines, `TobaccoIdentityDecision`-записи, batch 1.

**Тесты:** Vitest 956/956 (29 файлов, было 933/28); прирост +23 теста и +1 файл: `product-flavor-profile.test.ts` вырос с 27 до 33 тестов (+6, проверки состава batch 2/отсутствия пересечений/трёх новых sparse-кейсов), плюс новый файл `priority-queue.test.ts` (+17); `tsc --noEmit` — чисто; `eslint .` — чисто; `verify:product-flavor-profile` — 30 продуктов, 50 заполненных измерений, 103 evidence, confidence LOW/MEDIUM/HIGH = 15/15/0, HIGH не использован ни разу.

**Осталось:** заполнить оставшиеся 56 из 86 `RESOLVED` продуктов (batch 3 и далее, с новым manufacturer-tier tie-break); подключение registry к `calculateMixAnalysis`/scoring/UI — отдельная будущая итерация; локальный коммит batch 2 на ветке `feature/v0.3.5-product-flavor-profile-registry` ещё предстоит.

## 2026-07-23 — ADR-014: целевая модель из 7 измерений (по итогам анализа гипотетического Prisma-импорта)

**Сделано:** по запросу пользователя проведена (без кода) оценка гипотетического шага «создать записи `Flavor` в Postgres из decision registry», затем — оценка собираемости evidence по каждому из 18 сенсорных полей Prisma `Flavor`. По итогам оформлен `ADR-014`: целевая модель Product Flavor Profile Registry зафиксирована как 7 измерений (`sweetness`, `sourness`, `freshness`, `intensity`, `strength`, `heatResistance`, `juiciness`) вместо 18; остальные 11 полей (`cooling`, `creaminess`, `bitterness`, `dessertLevel`, `spiceLevel`, `floralLevel`, `herbalLevel`, `smokyLevel`, `dryness`, `naturalness`, `persistence`) явно исключены из типа `FlavorDimensionId`, а не отложены как nullable-опция. Дублирующее измерение `richness` переименовано в `intensity` (тот же концепт «насыщенность вкуса») — переименован только ключ в batch-1.ts/batch-2.ts, значения и evidence не пересчитывались. Prisma schema не менялась. Анализ кода/сравнения миксов в этой сессии не выполнялся — только ADR и типы registry.

**Файлы:**
- создано: `docs/adr/ADR-014-product-flavor-profile-dimension-scope.md`;
- изменено: `src/lib/product-flavor-profile/{types,constants,batch-1,batch-2}.ts` (`richness` → `intensity`, добавлены `strength`/`heatResistance`/`juiciness` в `FlavorDimensionId`/`FLAVOR_DIMENSION_IDS`), `docs/adr/README.md` (индекс), `docs/architecture/product-flavor-profile-registry.md` (ссылка на ADR-014, обновлённая модель данных);
- не изменено: Prisma schema/миграции, значения/evidence уже заполненных измерений batch 1–2, `TobaccoIdentityDecision`-записи.

**Тесты:** Vitest 956/956 без изменения количества (переименование ключа, не новые тесты); `tsc --noEmit` — чисто; `eslint .` — чисто; `verify:product-flavor-profile` — те же 30/50/103/{15,15,0}, что и до переименования (подтверждает: значения не пересчитывались).

**Осталось:** следующий исследовательский проход должен сначала добрать `strength`/`heatResistance`/`juiciness` для уже покрытых 30 продуктов batch 1–2, и только затем расширять на batch 3 (продукты 31+) по очереди с manufacturer-tier tie-break; вопрос фактической персистентности в Postgres остаётся отдельным будущим решением вне ADR-014.

## 2026-07-23 — Добор strength/heatResistance/juiciness для batch 1–2 (без начала batch 3)

**Сделано:** по прямому запросу добраны три новых измерения ADR-014 (`strength`, `heatResistance`, `juiciness`) для всех 30 уже покрытых продуктов batch 1–2, той же методологией evidence/confidence. Введено явное разделение по уровню атрибуции: per-SKU данные (HTReviews «официальная/пользовательская крепость», прямые цитаты про конкретный вкус) — `MEDIUM`; бренд-уровневые данные, перенесённые на конкретный продукт без отдельного подтверждения — `LOW` для `strength` (может отличаться между вкусами бренда) и `MEDIUM` для `heatResistance` (свойство обработки листа, обычно единое для линейки). Для BlackBurn/MustHave переиспользованы уже провалидированные evidence из `tobacco-profile`. `juiciness` заполнен только при прямом слове «сочный»/«juicy» — 5 из 30 продуктов. Для Hook и Husky `heatResistance` не найден и оставлен пустым (не выдуман). Batch 3 не начинался.

**Файлы:**
- изменено: `src/lib/product-flavor-profile/{batch-1,batch-2,product-flavor-profile.test}.ts` (новые dim-записи; обновлены sparse-тесты под уже не пустые `jam-spelaya-marakuiya`/`blackburn-almond-pear`/`husky-kiwano`/`urban-soul-pineapple`), `docs/architecture/product-flavor-profile-registry.md`, создан `docs/engine-changelog/v0.3.5-product-flavor-profile-registry-dimension-backfill.md`;
- не изменено: Prisma schema/миграции, состав batch (те же 30 `canonicalProductId`), `sweetness`/`sourness`/`freshness`/`intensity` — значения этих измерений не пересчитывались, только добавлены новые.

**Тесты:** Vitest 956/956 (без изменения количества — обновлены существующие проверки, новых тестовых файлов не добавлено); `tsc --noEmit` — чисто; `eslint .` — чисто; `verify:product-flavor-profile` — 30 продуктов, 113 заполненных измерений (было 50), 230 evidence (было 103), confidence LOW/MEDIUM/HIGH = 22/8/0 (было 15/15/0 — часть продуктов понижена с MEDIUM до LOW из-за новых низкоуверенных бренд-уровневых значений, `urban-soul-pineapple` и `husky-kiwano` наоборот повышены с LOW до MEDIUM, так как их первые измерения оказались MEDIUM-уверенными).

**Осталось:** batch 3 (продукты 31+) ещё не начат — следующий шаг по очереди с manufacturer-tier tie-break; локальный коммит этого добора ещё предстоит.

## 2026-07-23 — Batch 3 (30→45 из 86 RESOLVED), первый батч с manufacturer-tier tie-break

**Сделано:** посчитан фактический порядок очереди реальным кодом (`sortByPriorityQueue`/`comparePriorityQueueCandidates`) по всем 56 оставшимся `RESOLVED`-продуктам: 46 из 56 имели одинаковые primary-метрики `1/1/2`, и manufacturer-tier tie-break разрешил эту ничью в пользу Tier 1 (`Chabacco`, `Element`, `MustHave`, `Sapphire Crown`) целиком — все 15 позиций batch 3 оказались Tier 1. Заполнены 7 измерений по той же методологии для всех 15 продуктов. Учтена специфика линеек Chabacco Medium (крепость средне-лёгкая/жаростойкость высокая) vs Chabacco Mix (крепость лёгкая/жаростойкость низкая) — не усреднялись. Один явный конфликт источников (`chabacco-mix-fruktovyi-led`, розничное «средняя» против спецификации линейки «лёгкая») сохранён раздельно с LOW confidence и пометкой расхождения.

**Файлы:**
- создано: `src/lib/product-flavor-profile/batch-3.ts`, `docs/engine-changelog/v0.3.5-product-flavor-profile-registry-batch-3.md`;
- изменено: `src/lib/product-flavor-profile/{registry,index,product-flavor-profile.test}.ts`, `scripts/verify-product-flavor-profile.ts` (порог 30 → 45), `docs/architecture/product-flavor-profile-registry.md`, `docs/roadmap/README.md` (45/86 вместо 30/86);
- не изменено: Prisma schema/миграции, batch 1–2, существующие engines, `TobaccoIdentityDecision`-записи.

**Тесты:** Vitest 957/957 (29 файлов, было 956/29, +1 — новая проверка состава batch 3); `tsc --noEmit` — чисто; `eslint .` — чисто; `verify:product-flavor-profile` — 45 продуктов, 166 заполненных измерений (было 113), 336 evidence (было 230), confidence LOW/MEDIUM/HIGH = 32/13/0 (было 22/8/0), HIGH не использован ни разу.

**Осталось:** batch 4 начинается с ранга 16 очереди (`sapphire-crown-mejumi`, Tier 1) — оставшиеся 41 из 86 `RESOLVED` продуктов; подключение registry к `calculateMixAnalysis`/scoring/UI — отдельная будущая итерация; локальный коммит batch 3 ещё предстоит.

## 2026-07-23 — Batch 4 (45→60 из 86 RESOLVED), первый батч, пересекающий границу Tier 1/2/3

**Сделано:** посчитан фактический порядок очереди по всем 41 оставшимся `RESOLVED`-продуктам. Batch 4 содержит последний оставшийся продукт Tier 1 (`sapphire-crown-mejumi`), весь Tier 2 (`Brusko`, `Husky` ×4, `Overdose` ×2) и начало Tier 3 (`Banger`, `Dozaj`, `Duft` ×2, `Endorphin` ×2, `FAKE`). Заполнены 7 измерений той же методологией. Дважды явно исключены чужие evidence при риске ложной атрибуции: для `husky-passion-fruit` не использованы описания вкуса «Passion Fruit» других марок (Fumari, Serbetli, Spectrum, Bonche), встретившиеся в том же поиске; для `dozaj-mint` не использованы материалы о вкусе «Mint» другого бренда (Rave Tobacco) — итог для Dozaj остался без единого заполненного измерения. Один явный конфликт источников (`brusko-medium-tsitrusovyi-chai`: общее позиционирование бренда «средняя крепость» против спецификации позиции «лёгкая крепость») сохранён раздельно с LOW confidence.

**Файлы:**
- создано: `src/lib/product-flavor-profile/batch-4.ts`, `docs/engine-changelog/v0.3.5-product-flavor-profile-registry-batch-4.md`;
- изменено: `src/lib/product-flavor-profile/{registry,index,product-flavor-profile.test}.ts` (проверка отсутствия пересечений обобщена на все батчи, новый sparse-тест для `dozaj-mint`), `scripts/verify-product-flavor-profile.ts` (порог 45 → 60), `docs/architecture/product-flavor-profile-registry.md`, `docs/roadmap/README.md` (60/86 вместо 45/86);
- не изменено: Prisma schema/миграции, batch 1–3, существующие engines, `TobaccoIdentityDecision`-записи.

**Тесты:** Vitest 959/959 (29 файлов, было 957/29, +2 — новая проверка состава batch 4 и sparse-тест `dozaj-mint`); `tsc --noEmit` — чисто; `eslint .` — чисто; `verify:product-flavor-profile` — 60 продуктов, 208 заполненных измерений (было 166), 420 evidence (было 336), confidence LOW/MEDIUM/HIGH = 39/21/0 (было 32/13/0), HIGH не использован ни разу.

**Осталось:** batch 5 начинается с ранга 16 очереди (`hook-granatovyi`, Tier 3) — оставшиеся 26 из 86 `RESOLVED` продуктов; для линейки Husky по-прежнему нет данных по `heatResistance` ни для одного продукта; подключение registry к `calculateMixAnalysis`/scoring/UI — отдельная будущая итерация; локальный коммит batch 4 ещё предстоит.

## 2026-07-23 — v0.3.5 Product Flavor Profile Registry (пятый батч)

**Сделано:** заполнен пятый приоритетный батч — ещё 15 продуктов (ранги 1–15 среди 26 оставшихся `RESOLVED`), покрытие выросло с 60 до 75 из 86. Весь батч состоит из Tier 3 — среди оставшихся продуктов не осталось производителей Tier 1/2 с той же primary-метрикой `1/1/2`; 4 формально Tier 1-продукта (`MustHave`, `Sapphire Crown`) остались за пределами batch 5 из-за более низкого `occurrenceCount`. При подготовке очереди обнаружена и исправлена ошибка в служебном скрипте вычисления очереди (сопоставление с отчётом шло по несуществующему полю `sourceGroupId` вместо `groupId`) — на состав и порядок продуктов уже отгруженных батчей 3–4 это не повлияло, но справочная фраза «ранг 16 = `hook-granatovyi`» в changelog batch 4 была неверной; исправлена на `fake-mumbai-tea`. Дважды применено правило кросс-брендового исключения (evidence для `fake-mumbai-tea` не спутано с одноимённым вкусом бренда Chabacco). Для `take-pineapple` не найдено ни одного независимого описания вкуса — заполнен только `strength` на уровне бренда, с обоснованным отступлением от стандартного правила `LOW` для brand-level reuse (источник явно утверждает единую крепость по всей линейке).

**Файлы:**
- создано: `src/lib/product-flavor-profile/batch-5.ts`, `docs/engine-changelog/v0.3.5-product-flavor-profile-registry-batch-5.md`;
- изменено: `src/lib/product-flavor-profile/{registry,index,product-flavor-profile.test}.ts`, `scripts/verify-product-flavor-profile.ts`, `docs/architecture/product-flavor-profile-registry.md`, `docs/roadmap/README.md`, `docs/engine-changelog/v0.3.5-product-flavor-profile-registry-batch-4.md` (исправление ошибочной справочной фразы про ранг 16);
- не изменено: Prisma schema/миграции, batch 1–4, существующие engines, `TobaccoIdentityDecision`-записи.

**Тесты:** Vitest 961/961 (29 файлов, было 959/29, +2 — новая проверка состава batch 5 и sparse-тест `take-pineapple`); `tsc --noEmit` — чисто; `eslint .` — чисто; `verify:product-flavor-profile` — 75 продуктов, 243 заполненных измерения (было 208), 490 evidence (было 420), confidence LOW/MEDIUM/HIGH = 45/30/0 (было 39/21/0), HIGH не использован ни разу — все числа сверены вручную по каждому из 15 новых продуктов и совпали в точности.

**Осталось:** batch 6 (последний) начинается с ранга 16 очереди (`urban-soul-strawberry`, Tier 3) — оставшиеся 11 из 86 `RESOLVED` продуктов; для линейки Husky по-прежнему нет данных по `heatResistance` ни для одного продукта; подключение registry к `calculateMixAnalysis`/scoring/UI — отдельная будущая итерация; локальный коммит batch 5 ещё предстоит.

## 2026-07-23 — v0.3.5 Product Flavor Profile Registry (шестой батч, финальный — 86/86)

**Сделано:** заполнен шестой, последний батч — 11 оставшихся продуктов (ранги 1–11 очереди), покрытие выросло с 75 до **86 из 86** `RESOLVED` продуктов. Регистратура полностью заполнена: очередь приоритизации пуста, `scripts/verify-product-flavor-profile.ts` дополнен прямой сверкой (`assert.deepEqual`) множества `canonicalProductId` в registry с полным списком `RESOLVED`-решений Tobacco Identity Decision Registry — подтверждено, что ни один продукт не пропущен и ни один не выдуман. Этот батч впервые показал реальный эффект tier-tie-break внутри группы с полностью равными primary-метриками: 4 формально Tier 1-продукта (`MustHave`, `Sapphire Crown`), отложенные из batch 5 из-за более низкого `occurrenceCount`, встали впереди всех Tier 3-продуктов этой группы. Дважды исключено кросс-товарное смешение источников (`urban-soul-strawberry` — не спутан с миксом «Strawberry Kiwi Grapefruit»; `sarma-360-persik` — не спутан с розничным вариантом «Персик-Молоко»); один раз явное указание источника «не сочный профиль» использовано как обоснование не заполнять `juiciness`, а не как пропуск.

**Файлы:**
- создано: `src/lib/product-flavor-profile/batch-6.ts`, `docs/engine-changelog/v0.3.5-product-flavor-profile-registry-batch-6.md`;
- изменено: `src/lib/product-flavor-profile/{registry,index,product-flavor-profile.test}.ts`, `scripts/verify-product-flavor-profile.ts` (длина 86 + финальная сверка полноты покрытия), `docs/architecture/product-flavor-profile-registry.md` (регистратура отмечена завершённой), `docs/roadmap/README.md` (v0.3.5 обновлён на 86/86, «оставшиеся батчи» заменены на «подключение к scoring/UI»);
- не изменено: Prisma schema/миграции, batch 1–5, существующие engines, `TobaccoIdentityDecision`-записи.

**Тесты:** Vitest 962/962 (29 файлов, было 961/29, +1 — новая проверка состава batch 6); `tsc --noEmit` — чисто; `eslint .` — чисто; `verify:product-flavor-profile` — 86 продуктов, 271 заполненное измерение (было 243), 546 evidence (было 490), confidence LOW/MEDIUM/HIGH = 47/39/0 (было 45/30/0), HIGH не использован ни разу, множество canonicalProductId в registry совпадает один в один с полным списком RESOLVED-решений — все числа сверены вручную по каждому из 11 новых продуктов и совпали в точности.

**Осталось:** заполнение данными завершено (86/86). Дальнейшие шаги — отдельные будущие итерации, требующие собственного решения о приоритизации: подключение registry к `calculateMixAnalysis`/scoring/UI; возможное расширение evidence там, где источников пока не нашлось (например, `heatResistance` для всей линейки Husky); импорт данных в Postgres `Flavor` — ранее оценено как отдельная задача, требующая своего ADR из-за разрыва 7 vs 18 полей.

## 2026-07-23 — переоценка вопроса импорта в Postgres + ADR-015 (nullable secondary-поля)

**Сделано:** по запросу переоценён (только анализ, без кода) вопрос «импортировать ли 86/86 RESOLVED-продуктов Product Flavor Profile Registry в Prisma `Flavor`» — ранее (при охвате 15–30 продуктов) вывод был «не делать, слишком много обязательных полей без evidence». С полным охватом 86/86 обнаружено: точное распределение — ни один продукт не имеет всех 7 целевых измерений заполненными (медиана 3 из 7), только `strength` покрыт почти универсально (84/86); найдена конкретная, а не гипотетическая коллизия с demo-данными (`overdose-coffee`: demo `sweetness=3/heatResistance=7` против evidence-backed `sweetness=6/heatResistance=9`) и смысловой почти-дубль (`daily-hookah-slivochnyi-krem` vs demo `Сливки`). По итогу — оформлен **ADR-015**: сделать 11 «несобираемых» полей `FlavorProfile`/Prisma `Flavor` (`cooling`, `creaminess`, `bitterness`, `dryness`, `dessertLevel`, `spiceLevel`, `floralLevel`, `herbalLevel`, `smokyLevel`, `naturalness`, `persistence`) nullable, с явной политикой «`null` = не измерено, никогда не 0», обязательной для каждого потребляющего модуля. Аудитом реальных потребителей (не по памяти, построчным чтением) уточнён список: 2 модуля из первоначальной оценки оказались ложными срабатываниями (`compatibility-rules.ts`, 4 файла `flavor-knowledge/*` — совпадение по именам категорий нот, а не по числовым Prisma-полям), но обнаружен один пропущенный ранее реальный и критичный потребитель — `src/lib/flavors/validate-flavor.ts` (сегодня гарантированно отклоняет `null` как невалидное значение через `Number.isFinite`). Итоговый план по 9 реальным модулям (+ сам тип `FlavorProfile`) включает конкретные, уже найденные во время аудита точки риска молчаливого приведения `null` к `0` в JS-арифметике — включая одну потенциальную порчу пользовательского результата (`mix-result-presentation/index.ts` рисует `creaminess`/`bitterness` как готовое число без проверки на `null`) и одну потенциальную логическую ошибку (`analyze-profile-balance.ts`, правило «плоский профиль»: `null < 3` истинно в JS).

**Файлы:**
- создано: `docs/adr/ADR-015-flavor-profile-nullable-secondary-dimensions.md`;
- изменено: `docs/adr/README.md` (ADR-015 добавлен в индекс);
- не изменено: Prisma schema/миграции, `src/lib/flavors/*`, все 9 модулей из плана ADR-015, `src/lib/product-flavor-profile/*` — как и было явно запрошено, эта сессия ограничена только документом-решением, без кода.

**Тесты:** не запускались — изменений в коде нет.

**Осталось:** реализация ADR-015 (миграция схемы, правка `validate-flavor.ts` и 9 модулей) — отдельная будущая задача; отдельная будущая ADR/решение по объёму импорта 86 продуктов и политике реконсиляции для `overdose-coffee`/`daily-hookah-slivochnyi-krem` — явно вне рамок ADR-015.

## 2026-07-23 — реализация ADR-015: nullable secondary-поля FlavorProfile

**Сделано:** 11 полей (`cooling`, `creaminess`, `bitterness`, `dryness`, `dessertLevel`, `spiceLevel`, `floralLevel`, `herbalLevel`, `smokyLevel`, `naturalness`, `persistence`) сделаны nullable в Prisma `Flavor` (миграция `20260723120000_flavor_secondary_dimensions_nullable`) и в типе `FlavorProfile` (`src/lib/flavors/types.ts`: `FLAVOR_PROFILE_CORE_FIELDS`/`FLAVOR_PROFILE_SECONDARY_FIELDS`, `FlavorProfile = Record<Core,number> & Record<Secondary,number|null>`). Политика «`null` = не измерено, никогда не 0» применена явно во всех реальных потребителях.

**Важные технические находки при реализации (расходятся с первоначальной оценкой в самой ADR-015):**
- Аудит через `tsc --noEmit` (а не через grep) дал более точный список реальных потребителей: 2 файла из первоначального списка ADR-015 оказались ложными срабатываниями (`mix-compatibility/compatibility-rules.ts`, 4 файла `flavor-knowledge/*`), зато нашлись 4 ранее не замеченных реальных потребителя: `mix-profile/calculate-weighted-profile.ts`, `mix-profile/validate-input.ts`, `mix-recommendation/calculate-recommendations.ts`, `mix-recommendation/helpers.ts` (`sourceComponentForCharacteristic`), плюс `prisma/verify-seed.ts` и `src/app/result/[id]/page.tsx`.
- Обнаружено, что `canonical-mix-scoring/build-effective-tobacco-profile.ts` уже содержит каскадный резолвер (`Number.isFinite` correctly исключает `null`, откатываясь на `CANONICAL_TECHNICAL_PROFILE` → `NEUTRAL_FALLBACK=5`), который резолвит `null` в реальное число **до** того, как профиль доходит до `calculateMixProfile`/`analyze-profile-balance`/`summary-tags`/`mix-result-presentation`. Из-за этого большинство добавленных guard'ов в этих модулях — правки уровня типов/явного самодокументирования, а не исправление живого рантайм-бага в основном пайплайне `calculateMixAnalysis`. Реальный, никем не резолвимый риск был сосредоточен в устаревшем `mix-analyzer.ts` (получает «сырые» Prisma-данные напрямую через `mapFlavor`, без каскада) и на валидационных границах (`validate-flavor.ts`, `validate-input.ts`, `calculate-recommendations.ts`).
- Найдено и исправлено 2 реальных поведенческих бага (не просто type-safety): `mix-compatibility/analyze-profile-balance.ts` — `dessert-balance` (`bitterness<=6`) и `fresh-juicy` (`dryness<=T.maxComfortableDryness`) — здесь `null` коэрсится в `0`, что тривиально удовлетворяет `<=`, то есть без explicit-guard'а неизмеренное поле молча «проходило» бы порог как заведомо низкое.
- Правило `profile.flat` (`every(value<3)`), заранее названное в самой ADR-015 «опасным», при точном разборе оказалось математически эквивалентно корректной обработке: `null<3` в JS всегда `true`, что даёт тот же итог, что и явное исключение `null` из проверки. Guard добавлен для явности и на случай будущего рефакторинга, но заявленный риск для этого конкретного правила не подтвердился — исправлено в понимании, задокументировано в коммите.
- В `mix-recommendation/helpers.ts` `sourceComponentForCharacteristic` найден настоящий edge-case: без explicit-исключения `null` компонент с реальным измеренным `0` и компонент с `null` получали одинаковый «счёт» (оба ноль) и могли перепутаться местами в выборе «источника» характеристики для рекомендации.

**Файлы:**
- создано: `prisma/migrations/20260723120000_flavor_secondary_dimensions_nullable/migration.sql`, `src/lib/canonical-mix-scoring/calculate-canonical-mix-score.test.ts`;
- изменено: `prisma/schema.prisma`, `prisma/verify-seed.ts`, `src/app/result/[id]/page.tsx`, `src/lib/flavors/{types,validate-flavor,validate-flavor.test}.ts`, `src/lib/mix-profile/{calculate-weighted-profile,validate-input,mix-profile.test}.ts`, `src/lib/canonical-mix-scoring/calculate-canonical-mix-score.ts`, `src/lib/mix-compatibility/{analyze-profile-balance,summary-tags,compatibility.test}.ts`, `src/lib/mix-recommendation/{calculate-recommendations,helpers,profile-recommendations,recommendation.test}.ts`, `src/lib/mix-result-presentation/{index,mix-result-presentation.test}.ts`, `src/lib/mix-analyzer.ts`, `src/lib/mix-analyzer.test.ts`;
- не изменено: `src/lib/product-flavor-profile/*` (7-полевая модель ADR-014 не связана с этой ADR), `prisma/seed.ts` (демо-данные остаются полностью заполненными, nullable ≠ обязан быть null).

**Тесты:** Vitest **999/999 passed, 30 файлов** (было 962/29, +37 новых тестов на null-случаи во всех затронутых модулях); `tsc --noEmit` — чисто; `eslint .` — чисто; `npx prisma validate` — схема валидна; миграция применена к локальному Postgres в Docker (`npx prisma migrate deploy` + `npx prisma generate`) в момент реализации — на момент финальной проверки Docker Desktop оказался недоступен на машине (не связано с изменениями кода).

**Осталось:** объём импорта 86 продуктов Product Flavor Profile Registry в Postgres и политика реконсиляции для `overdose-coffee`/`daily-hookah-slivochnyi-krem` — по-прежнему отдельное будущее решение, вне рамок ADR-015.

## 2026-07-23 — ADR-016: политика импорта registry в Prisma и реконсиляции с demo-данными

**Сделано:** оформлена **ADR-016**, закрывающая 4 явных вопроса, оставленных открытыми в ADR-015: (1) порог заполненности данных не вводится — импортируются все 86 продуктов независимо от того, сколько из 7 измерений заполнено (0–6, ни одного с 7/7); вместо порога — отдельная, независимая от `overallConfidence` ось `dataCompleteness` (`DETAILED`/`GOOD`/`BASIC`/`MINIMAL` по числу заполненных измерений: 6–7/4–5/2–3/0–1); (2) при коллизии с demo-строкой Prisma evidence-backed значение registry всегда перезаписывает поле при апсерте — demo-данные без evidence не являются равноправным источником; (3) `catalogEntryType: TEST` (`Test Kitchen`, 8 строк) исключается из публичной выдачи `/catalog` и `/builder` явным фильтром, но остаётся в базе для `verify-seed.ts` и тестов; (4) смысловые почти-дубли (совпадающие по смыслу, но разные по `slug`/названию) не объединяются автоматически — уходят в отдельный review-список по аналогии с P0/P1-процессом Tobacco Identity Decision Registry.

По явному ограничению задачи сам импорт-скрипт и миграция под возможную будущую колонку `Flavor.dataCompleteness` не реализованы — выполнены только декларативные части, безопасные для немедленного применения независимо от того, когда состоится сам импорт.

**Файлы:**
- создано: `docs/adr/ADR-016-catalog-import-and-demo-reconciliation-policy.md`, `docs/architecture/product-flavor-profile-demo-near-duplicates.md` (review-список — на сегодня 1 подтверждённая пара: `daily-hookah-slivochnyi-krem` ↔ demo `Daily Hookah/Сливки`, плюс 4 явно проверенных и отклонённых бренда), `src/lib/product-flavor-profile/data-completeness.ts` (`calculateDataCompleteness`, `countFilledDimensions`);
- изменено: `docs/adr/README.md` (ADR-016 в индексе), `src/lib/product-flavor-profile/{types,constants,public-mapper,index,product-flavor-profile.test}.ts` (`DataCompletenessLevel` в `PublicProductFlavorProfile`, вычисляется в `mapProductFlavorProfileToPublic`, не хранится), `src/app/catalog/page.tsx` и `src/app/builder/page.tsx` (явный фильтр `catalogEntryType: { not: "TEST" }` в публичных запросах);
- не изменено: сам импортёр (не существует и не создан в этой сессии), `prisma/schema.prisma`/миграции (никакая новая колонка не добавлена — `dataCompleteness` только вычисляемый), `prisma/seed.ts`.

**Тесты:** Vitest **1010/1010 passed, 30 файлов** (было 999/30, +11 новых — покрытие всех 8 границ `calculateDataCompleteness`, проверка независимости от `overallConfidence`, сверка реального распределения по всем 86 продуктам с цифрами, заявленными в самой ADR — `DETAILED: 4, GOOD: 28, BASIC: 47, MINIMAL: 7`, совпало в точности); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** сам импортёр (upsert 86 продуктов в `Flavor`, реализация п. 2 «evidence всегда побеждает» и п. 4 «почти-дубли не объединяются») — отдельная будущая задача; возможное превращение `dataCompleteness` в хранимую Prisma-колонку — отдельная будущая миграция, если возникнет такая потребность.

## 2026-07-23 — ADR-017: nullable core-поля FlavorProfile (расширение ADR-015)

**Сделано:** при проектировании импортёра Product Flavor Profile Registry (ADR-016) обнаружено блокирующее противоречие: требование «импортировать все 86 продуктов независимо от заполненности (0–6 из 7, ни одного с 7/7)» несовместимо с тем, что 7 core-полей ADR-014 (`strength`, `heatResistance`, `intensity`, `sweetness`, `acidity`, `juiciness`, `freshness`) в Prisma `Flavor` оставались обязательными (не nullable) — ADR-015 сделала nullable только 11 второстепенных полей. Вопрос вынесен на явное решение пользователя (три варианта: сделать core тоже nullable / импортировать только полные профили — 0 из 86 продуктов / подставлять заглушку). Выбран первый вариант, оформлен как **ADR-017**: все 18 полей теперь nullable, `FlavorProfile` упрощён обратно до единого `Record<Field, number|null>` (различие core/secondary сохранено только как справочные константы для ADR-014/registry). Аудит потребителей повторён тем же методом, что и для ADR-015 — через `tsc --noEmit` как источник истины, а не по памяти: найдено значительно больше мест, чем в первом проходе (73 ошибки против 49 у ADR-015), так как `intensity`/`sweetness`/`acidity`/`freshness` использовались практически во всех формулах scoring/compatibility/recommendation (доминирование, вклад нот, пороговые правила). Введена общая функция `resolveIntensity`/`NEUTRAL_INTENSITY_FALLBACK=5` (`src/lib/flavors/types.ts`) для мест, где нужна гарантированно сравнимая интенсивность (ранжирование доминирования), по аналогии с уже существующим `NEUTRAL_FALLBACK` в `canonical-mix-scoring`. Обнаружены и исправлены несколько реальных (не type-safety-only) поведенческих рисков в `<=`-направленных правилах `analyze-profile-balance.ts` (`profile.cloying`, `profile.sharp-acidity`, `profile.sweet-low-freshness`) и `summary-tags.ts` (`LIGHT`), где `null` в JS молча удовлетворяет `<=` (0 ≤ порог).

**Файлы:**
- создано: `prisma/migrations/20260723130000_flavor_core_dimensions_nullable/migration.sql`, `docs/adr/ADR-017-flavor-profile-nullable-core-dimensions.md`;
- изменено: `prisma/schema.prisma`, `docs/adr/README.md`, `src/lib/flavors/{types,validate-flavor,validate-flavor.test}.ts`, `src/lib/mix-profile/{calculate-weighted-profile,calculate-component-influence,calculate-note-contributions,validate-input,mix-profile.test}.ts`, `src/lib/mix-compatibility/{types,summary-tags,analyze-profile-balance,compatibility.test}.ts`, `src/lib/mix-recommendation/{calculate-recommendations,dominance-recommendations,profile-recommendations,recommendation.test}.ts`, `src/lib/canonical-mix-scoring/{build-effective-tobacco-profile,calculate-canonical-mix-score}.ts`, `src/lib/mix-analysis/{calculate-mix-analysis,mix-analysis.test}.ts`, `src/lib/mix-analyzer.ts`, `src/lib/mix-result-presentation/index.ts`, `scripts/{verify-mix-compatibility,verify-mix-recommendation}.ts`;
- не изменено: `src/lib/product-flavor-profile/*`, `prisma/seed.ts` (demo-данные остаются полностью заполненными).

**Тесты:** Vitest **1023/1023 passed, 30 файлов** (было 1010/30, +13 новых на null-случаи для новых nullable core-полей); `tsc --noEmit` — чисто; `eslint .` — чисто; `npx prisma validate` — схема валидна, `npx prisma generate` выполнен успешно. `npx prisma migrate deploy` к реальной локальной базе **не выполнен** в этой сессии — Docker Desktop недоступен на машине на момент проверки (не связано с изменениями кода); миграция написана и синтаксически корректна, будет применена при следующем поднятии Docker.

**Осталось:** применить миграцию `20260723130000_flavor_core_dimensions_nullable` к реальной базе, когда Docker будет доступен; сам импортёр Product Flavor Profile Registry (ADR-016) теперь разблокирован и является следующим шагом той же сессии.

## 2026-07-23 — импортёр Product Flavor Profile Registry (ADR-016), реализация

**Сделано:** написан `scripts/import-product-flavor-profiles.ts` — загружает 86 `RESOLVED`-продуктов из `TOBACCO_IDENTITY_DECISION_REGISTRY`, берёт их 7 evidence-backed измерений из `PRODUCT_FLAVOR_PROFILE_REGISTRY`, апсертит в Prisma `Flavor` по политике ADR-016. Вся логика планирования (какое действие — CREATE/UPDATE/SKIPPED_TEST, какие поля меняются, какие предупреждения) вынесена в чистый, не обращающийся к Prisma модуль `src/lib/product-flavor-profile-import/` — по образцу `createCatalogIdentityBackfillPlan`, чтобы логику можно было протестировать без реальной базы. Сам скрипт — тонкая I/O-обёртка: читает существующие строки, вызывает планировщик, печатает отчёт, и только с `--apply` пишет.

Реализованы все 4 пункта политики: (1) порога заполненности нет, импортируются все 86 продуктов, `dataCompleteness` попадает только в отчёт скрипта (Prisma-колонки для него по-прежнему нет — вычисляется на лету); (2) evidence-backed 7 core-полей всегда перезаписывают demo-значения при апсерте, причём буквально — если у registry для поля нет evidence, поле уходит в `null` даже поверх реального demo-числа (уточнено и явно задокументировано как addendum к ADR-016, так как исходный текст ADR этот нюанс не оговаривал); 11 второстепенных полей и `name`/`description`/`profileStatus`/`profileSource`/`catalogEntryType` существующей строки при `UPDATE` не трогаются никогда; (3) `TEST`-записи полностью пропускаются (`SKIPPED_TEST`), импортёр их не создаёт и не обновляет; (4) единственный известный смысловой почти-дубль (`daily-hookah-slivochnyi-krem` ↔ demo «Сливки») создаётся отдельной строкой с явным предупреждением в отчёте, без автослияния.

Дополнительно решены и задокументированы addendum'ом к ADR-016 два вопроса, не покрытых исходным текстом: шаблон `description` (канонический идентификатор без выдуманной прозы о вкусе) и выбор `profileStatus: DRAFT` / `profileSource: MIXED` как наименее вводящей в заблуждение комбинации.

Поскольку Docker/Postgres не был доступен в момент реализации (та же причина, что и в предыдущих двух записях), настоящий `--dry-run` против живой базы не запускался. Вместо этого построен и показан пользователю офлайн-предпросмотр: чистая функция планирования вызвана для всех 86 продуктов с `existingFlavor`, подставленным напрямую из буквального содержимого `prisma/seed.ts` (без реального запроса к БД) — результат: **85 CREATE, 1 UPDATE, 0 SKIPPED_TEST**; единственная коллизия — `overdose-coffee`; единственное предупреждение о почти-дубле — `daily-hookah-slivochnyi-krem`. Этот предпросмотр верен только при допущении, что база сейчас содержит ровно исходные 16 seed-строк и ничего больше — не заменяет реальный `--dry-run`. `--apply` не запускался и не будет запущен без отдельного явного подтверждения пользователя.

**Файлы:**
- создано: `src/lib/product-flavor-profile-import/{types,build-import-plan,build-import-plan.test,index}.ts`, `scripts/import-product-flavor-profiles.ts`;
- изменено: `package.json` (новый скрипт `import:product-flavor-profiles`), `docs/adr/ADR-016-catalog-import-and-demo-reconciliation-policy.md` (addendum с решением по `description`/`profileStatus`/`profileSource`/политике перезаписи в `null`, плюс зафиксированные офлайн-цифры 85/1/0);
- не изменено: Prisma schema/миграции (импортёр только пишет данные существующими Prisma-моделями, не меняет их), `prisma/seed.ts`, `docs/architecture/product-flavor-profile-demo-near-duplicates.md` (уже содержал нужную пару).

**Тесты:** Vitest **1041/1041 passed, 31 файл** (было 1023/30, +18 новых в `build-import-plan.test.ts` — все переходы CREATE/UPDATE/SKIPPED_TEST, политика перезаписи демо-данных на реальных цифрах `overdose-coffee` из настоящего registry, маппинг `sourness→acidity`, явное предупреждение о почти-дубле, независимость `dataCompleteness` от прочей логики); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** запустить настоящий `--dry-run` против реальной базы, когда Docker будет доступен, и сверить его с офлайн-предпросмотром; `--apply` — только по отдельному явному запросу пользователя после того, как он увидит реальный (не офлайн) отчёт.

## 2026-07-24 — реальный `--dry-run`/`--apply` импортёра против живой базы

**Сделано:** Docker оказался уже поднят (`hookah-mix-postgres` healthy); `npx prisma migrate status` показал неприменённую миграцию ADR-017 (`20260723130000_flavor_core_dimensions_nullable`). Реальный `--dry-run` (только чтение, до применения миграции) дал результат, **полностью идентичный** офлайн-предпросмотру из предыдущей записи: 85 CREATE / 1 UPDATE / 0 SKIPPED_TEST, `dataCompleteness` DETAILED:4/GOOD:28/BASIC:47/MINIMAL:7, коллизия `overdose-coffee`, предупреждение о `daily-hookah-slivochnyi-krem` — подтверждает, что живая база на тот момент содержала ровно исходные seed-данные без расхождений. По явному запросу пользователя применена миграция (`npx prisma migrate deploy`) и выполнен `--apply`. Итог в базе подтверждён прямыми SQL-запросами: было 17 строк `Flavor` (8 `REAL` + 9 `TEST`), стало 102 (93 `REAL` + 9 `TEST`, `TEST`-строки не тронуты); `overdose-coffee` (id=1) получил ровно заявленные изменения (`heatResistance` 7→9, `sweetness` 3→6, `intensity`/`acidity`/`juiciness`/`freshness` → `null`, `strength=8` не менялся). Поднят dev-сервер (создан `.claude/launch.json`), `/builder` открыт в браузере — список показывает ровно 93 табака, `TEST`-записи не отображаются, новые продукты (включая `Daily Hookah — Сливочный крем` рядом со старой demo-строкой `Сливки`) присутствуют.

**Файлы:**
- создано: `.claude/launch.json` (конфигурация dev-сервера для превью, не относится к домену приложения);
- изменено: живая база Postgres (не файл в репозитории) — 85 новых строк `Flavor` создано, 1 обновлена;
- не изменено: код проекта (эта запись — только применение уже написанных миграции и импортёра, без правок логики).

**Тесты:** отдельно не перезапускались (код не менялся) — прямая проверка через SQL-запросы к базе и визуальную проверку `/builder` в браузере.

**Осталось:** обнаружена — по отдельному запросу пользователя — узкая проблема диапазона `predictedQualityScore` (7.5–7.8 на выборке из 12 пар новых продуктов, диагностика описана в переписке); корень — отсутствие `FlavorNoteAssignment` у всех 86 импортированных продуктов, вынесено в ADR-018 (следующая запись).

## 2026-07-24 — ADR-018: перенос `dominantNoteIds` в `FlavorNoteAssignment` (только план, без кода)

**Сделано:** по запросу пользователя выполнена диагностика причины узкого диапазона scoring на новых 86 продуктах: у **всех** импортированных строк `Flavor` — 0 записей `FlavorNoteAssignment` (перенос `dominantNoteIds` не входил в объём импортёра ADR-016), из-за чего `buildEffectiveTobaccoProfile` для каждой из них подставляет синтетическую категорию `OTHER` (`reliabilityScore: 15`), и `noteCompatibility` (40% веса `compatibilityScore`) остаётся замороженным на базовом значении `7` для всех пар без исключения — это и объясняет узкий диапазон 7.5–7.8 (уже, чем 7.5–7.9 из v0.3.3). При проектировании переноса обнаружены два дополнительных факта, не видимых на уровне ADR-016: (1) `dominantNoteIds` использует более широкий словарь категорий `flavor-knowledge` (25 значений) чем Prisma `FlavorNoteCategory` (16 значений) — 9 из 24 фактически используемых в реестре категорий (`SOUR`, `CANDY`, `MINT`, `TEA`, `FRESH`, `BAKERY`, `ALCOHOL`, `WOODY`, `VANILLA`; 52 из 184 объявлений, ≈28%) не имеют аналога в Prisma-enum вообще; (2) из 23 связей в `CATEGORY_RELATIONS` (включая проверенное отсутствие `FLORAL+CREAMY` в любом направлении) реально подключены к скорингу через `NOTE_COMPATIBILITY_RULES` только 2 — остальные не влияют на `compatibilityScore` независимо от того, есть они в `CATEGORY_RELATIONS` или нет.

Оформлена **ADR-018**: (1) перенос реализуется отдельным скриптом (`scripts/import-product-flavor-note-assignments.ts` + чистый планировщик `src/lib/product-flavor-profile-note-import/`), не расширением уже сданного основного импортёра; (2) `FlavorNoteCategory` enum расширяется 9 новыми значениями аддитивной миграцией вместо lossy-маппинга в ближайшую существующую категорию; (3) для каждой из 24 категорий — своя generic-нота (8 переиспользуются из существующих demo-нот, остальные создаются новыми, `noteType: DOMINANT`/`intensity: 10` по уже сложившейся конвенции); (4) добавление новых связей в `CATEGORY_RELATIONS` (например `FLORAL+CREAMY`) явно вынесено за рамки этой ADR — content-решение, требующее отдельного обоснования; (5) честная количественная оценка ожидаемого эффекта — `usedFallback` по числовым полям не изменится (все 11 secondary всё ещё `null`), но категории перестанут быть `OTHER`, что даст `noteCompatibility` прямое плечо влияния до ≈10% шкалы `predictedQualityScore`; диапазон должен расшириться, но не для всех пар одинаково — только там, где есть подключённое правило.

**Файлы:**
- создано: `docs/adr/ADR-018-product-flavor-profile-note-category-backfill.md`;
- изменено: `docs/adr/README.md` (ADR-018 в индексе);
- не изменено: код (по явному ограничению задачи — только ADR и план в этой сессии).

**Тесты:** не запускались — сессия не меняла код.

**Осталось:** реализация ADR-018 (миграция enum, планировщик, скрипт, тесты) — отдельная будущая задача; отдельная будущая ADR по конкретным новым записям `CATEGORY_RELATIONS`/`NOTE_COMPATIBILITY_RULES` (в т.ч. `FLORAL+CREAMY`) — вне объёма ADR-018.

## 2026-07-24 — ADR-018, шаг 1: расширение Prisma-enum `FlavorNoteCategory` (только миграция, без переноса категорий)

**Сделано:** по явному запросу пользователя реализован только первый, декларативный шаг ADR-018 — расширение Prisma-enum `FlavorNoteCategory` девятью значениями (`SOUR`, `CANDY`, `MINT`, `TEA`, `FRESH`, `BAKERY`, `ALCOHOL`, `WOODY`, `VANILLA`), без самого переноса `dominantNoteIds` (планировщик/скрипт/generic-ноты по-прежнему не написаны — отдельная будущая задача). Миграция `20260724000000_flavor_note_category_add_values` — девять `ALTER TYPE ... ADD VALUE`, применена к живой базе (`prisma migrate deploy`). Проверено напрямую через `pg_enum`: все 16 исходных значений сохранили свой порядок (sortorder 1–16), 9 новых добавлены строго в конец (17–25), ни одно не переименовано/удалено; все 27 существующих строк `FlavorNote` не тронуты.

Расширение enum сломало `tsc --noEmit` (13 ошибок в 6 файлах: `prisma/seed.ts`, `scripts/verify-mix-{compatibility,profile}.ts`, `src/app/api/mixes/route.ts`, `src/app/builder/page.tsx`, `src/app/result/[id]/page.tsx`) — причина одна и та же везде: ручной (не генерируемый из Prisma) TS-тип `FlavorNoteCategory` в `src/lib/flavors/types.ts` (`FLAVOR_NOTE_CATEGORIES`) остался на старых 16 значениях, и на границе, где реальные `Flavor.notes` из Prisma передаются в функции с этим типом, TypeScript увидел несовместимость. Это ожидаемый, задокументированный в самой ADR-018 (Consequences) эффект, а не сюрприз. Пользователю показан полный список ошибок и предложен явный выбор (`AskUserQuestion`): расширить константу сейчас (чисто механическая синхронизация типа со схемой, не логика переноса категорий) или оставить сборку красной до отдельной сессии реализации ADR-018. Выбрано первое — `FLAVOR_NOTE_CATEGORIES` расширена теми же 9 значениями.

**Файлы:**
- создано: `prisma/migrations/20260724000000_flavor_note_category_add_values/migration.sql`;
- изменено: `prisma/schema.prisma` (enum `FlavorNoteCategory`, +9 значений, аддитивно), `src/lib/flavors/types.ts` (`FLAVOR_NOTE_CATEGORIES`, синхронизирована с Prisma-enum — только константа, без логики переноса notes);
- не изменено: `src/lib/flavor-knowledge/note-taxonomy.ts` (`LEGACY_CATEGORY_MAP`/`toKnowledgeCategory` для 9 новых категорий пока не расширены — это часть самой реализации ADR-018, а не этого шага), `src/lib/product-flavor-profile-note-import/` (не создан — планировщик/скрипт переноса `dominantNoteIds` остаются отдельной будущей задачей), demo-данные `prisma/seed.ts`.

**Тесты:** Vitest **1041/1041 passed, 31 файл** (без изменений — новые категории пока нигде не используются); `tsc --noEmit` — чисто после правки константы; `eslint .` — чисто; `npx prisma validate` — схема валидна.

**Осталось:** сама реализация ADR-018 — `LEGACY_CATEGORY_MAP` для 9 новых категорий, чистый планировщик `src/lib/product-flavor-profile-note-import/`, скрипт `scripts/import-product-flavor-note-assignments.ts` (`--dry-run`/`--apply`), generic-ноты (8 переиспользовать, ~16 создать новыми), тесты — по явному указанию пользователя не начата в этой сессии.

## 2026-07-24 — ADR-018: реализация переноса `dominantNoteIds` (планировщик + скрипт, без `--apply`)

**Сделано:** реализован сам перенос категорий по плану ADR-018. `LEGACY_CATEGORY_MAP` (`src/lib/flavor-knowledge/note-taxonomy.ts`) расширена 9 identity-парами для новых Prisma-категорий; добавлена обратная функция `toLegacyCategory` (строится инвертированием `LEGACY_CATEGORY_MAP`, а не дублированием таблицы вручную — исключает рассинхронизацию направлений). Новый чистый (без обращений к Prisma) планировщик `src/lib/product-flavor-profile-note-import/` (`types.ts`, `build-note-import-plan.ts`, `index.ts`) содержит статическую таблицу `GENERIC_NOTE_DEFINITIONS` (24 категории → generic-нота: 8 переиспользуют уже существующие demo-ноты `fruit`/`berry`/`citrus`/`dessert`/`spice`/`herbal`/`tropical`/`coffee`, 16 — новые, включая `mint-generic`/`vanilla-generic` с отдельными от существующих специфичных нот `mint`/`vanilla` slug) и функцию `buildNoteImportPlan`, реализующую идемпотентную логику: пропуск `TEST`-строк и продуктов без `dominantNoteIds`, дедупликация повторных категорий внутри одного продукта, отдельное предупреждение (не падение) для категории без generic-ноты, отсутствие повторной записи уже существующего назначения `flavorId+noteId` при повторном запуске.

Скрипт `scripts/import-product-flavor-note-assignments.ts` — тонкая I/O-обёртка по образцу `import-product-flavor-profiles.ts`: `--dry-run` по умолчанию / `--apply` реальная запись, `--json` для машиночитаемого вывода; при `--apply` сначала создаёт (`upsert` по `slug`) недостающие generic-ноты, затем назначения (`upsert` по составному `flavorId_flavorNoteId`) — обе операции идемпотентны при повторном запуске. `CATEGORY_RELATIONS`/`NOTE_COMPATIBILITY_RULES` не затронуты вообще, по явному указанию пользователя — это отдельная будущая задача (ADR-018 п.4).

Реальный `--dry-run` против живой базы (Docker/Postgres доступен) дал: **16 новых generic-нот**, **183 новых назначения**, **1 уже назначено** (`overdose-coffee`/`COFFEE` — demo-нота `coffee` совпала с evidence-backed категорией), **0 пропущено** по всем причинам (`NO_FLAVOR_FOUND`/`TEST_ENTRY`/`NO_DOMINANT_NOTES`), **0 предупреждений**. 183+1=184 — совпадает точно с независимо посчитанным в ADR-018 общим числом объявлений `dominantNoteIds` по всем 86 продуктам, что подтверждает отсутствие двойного учёта или потерь. `--apply` не запускался — по явному указанию пользователя требует отдельного подтверждения, как и в основном импортёре.

**Файлы:**
- создано: `src/lib/product-flavor-profile-note-import/{types,build-note-import-plan,build-note-import-plan.test,index}.ts`, `scripts/import-product-flavor-note-assignments.ts`;
- изменено: `src/lib/flavor-knowledge/note-taxonomy.ts` (`LEGACY_CATEGORY_MAP` +9 пар, `toLegacyCategory`), `src/lib/flavor-knowledge/index.ts` (экспорт `toLegacyCategory`), `src/lib/flavor-knowledge/knowledge.test.ts` (+4 теста на обратный маппинг), `package.json` (новый скрипт `import:product-flavor-note-assignments`);
- не изменено: `src/lib/flavor-knowledge/category-relations.ts`, `src/lib/mix-compatibility/compatibility-rules.ts` (`CATEGORY_RELATIONS`/`NOTE_COMPATIBILITY_RULES` — явно вне объёма этой задачи), Prisma-схема (enum уже расширен в предыдущей записи, здесь только данные).

**Тесты:** Vitest **1059/1059 passed, 32 файла** (было 1041/31, +18: 14 в `build-note-import-plan.test.ts` — включая реальный кейс `overdose-coffee` из registry, +4 в `knowledge.test.ts` на `toLegacyCategory`); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** запустить `--apply` — только по отдельному явному подтверждению пользователя, как и требовалось; после этого ожидаемо: категории у всех 86 продуктов перестанут быть синтетическим `OTHER` в `buildEffectiveTobaccoProfile`, `noteCompatibility` получит реальный сигнал для пар с уже подключёнными правилами `NOTE_COMPATIBILITY_RULES` (см. диагностику из предыдущей сессии); добавление новых правил в `CATEGORY_RELATIONS`/`NOTE_COMPATIBILITY_RULES` (например `FLORAL+CREAMY`) — отдельная будущая задача, сознательно не начатая.

## 2026-07-24 — ADR-019: импорт внешнего исследования совместимости категорий (22 из 29 пар)

**Сделано:** пользователь предоставил batch из 29 пар категорий с кодом совместимости (`STRONG`/`GOOD`/`CONDITIONAL`/`NEUTRAL`/`RISKY`) и уровнем доказательности (`HIGH`/`MEDIUM`/`LOW`) от внешнего агрегированного веб-исследования. Перед реализацией все 29 пар программно сверены с уже существующими 24 записями `CATEGORY_RELATIONS` (не на глаз — первый ручной проход пропустил один конфликт, пришлось пересчитать скриптом): найдено **5 конфликтов** (`TEA+CITRUS`, `DAIRY+BERRY`, `TEA+SPICE`, `MINT+DAIRY`, `COFFEE+VANILLA` — новые данные противоречат уже существующим hand-authored записям, включая 2 полных разворота знака) и **2 точных дубля** (`FLORAL+BERRY`, `DAIRY+DESSERT` — новое исследование подтверждает уже существующие записи без изменений). Пользователю явно показана таблица конфликтов через `AskUserQuestion`; выбрано: старые записи не перезаписываются, конфликтные пары не импортируются, фиксируются в ADR как review-список для будущего осознанного решения.

Добавлен новый evidence type `AGGREGATED_RESEARCH` (`src/lib/flavor-knowledge/types.ts`/`constants.ts`) — явно отделён от `EDITORIAL_RESEARCH`/`INTERNAL_EXPERT_RULE`/`VERIFIED_TEST`, обозначает «LLM-агрегированный веб-поиск с реальными ссылками, не прямая ручная проверка». 4 evidence-записи по уровням доказательности: `HIGH`=0.7, `MEDIUM`=0.5, `LOW`=0.3, и отдельный `single-product-marketing`=0.15 — специально для 3 из 4 пар, единственный источник которых оказался розничным описанием одного товара (Deus Vanilla Berries); этот вес — самый низкий во всей таблице evidence проекта (был 0.3), что подтверждено отдельным тестом.

Код совместимости смаплен на `FlavorCategoryRelationType` по уже существующей шкале `baseScore` (`STRONG`→`STRONG_MATCH`=0.7, `GOOD`→`GOOD_MATCH`=0.4, `CONDITIONAL`→`COMPLEMENTARY_CONTRAST`=0.25, `RISKY`→`RISKY`=-0.4) — сознательно без дополнительного масштабирования по уровню доказательности: confidence выражается только через evidence weight, два измерения не смешиваются в одно число. Добавлено 22 новые записи в `CATEGORY_RELATIONS`, из них 21 подключена через `createKnowledgeCategoryRule` в `NOTE_COMPATIBILITY_RULES` (реально влияют на scoring); 22-я (`CITRUS+SPICE`, `NEUTRAL`) записана для provenance («проверили — нейтрально», а не «не проверяли»), но не подключена — `createKnowledgeCategoryRule` намеренно бросает исключение для `NEUTRAL`.

Особо отмечена и подключена `FLORAL+CREAMY` (`CONDITIONAL`/`LOW`) — именно эта связь отсутствовала в диагностике пары `Sarma — 360 Крепкая Горная лаванда` + `Daily Hookah — Сливки` из предыдущей сессии. Повторная диагностика на реальных данных из живой базы: `noteCompatibility.score` 7.0→**7.3**, `predictedQualityScore` 7.5→**7.6** — скромный, но реальный и пропорциональный слабой (`LOW`) доказательности сдвиг (`category.floral-creamy` появляется в `appliedRules`/`positiveFactors`).

**Файлы:**
- создано: `docs/adr/ADR-019-aggregated-research-category-compatibility-rules.md`;
- изменено: `src/lib/flavor-knowledge/types.ts` (`AGGREGATED_RESEARCH`), `src/lib/flavor-knowledge/constants.ts` (`EVIDENCE_TYPES`), `src/lib/flavor-knowledge/evidence.ts` (4 новые evidence-записи), `src/lib/flavor-knowledge/category-relations.ts` (`researched()` helper + 22 новые записи), `src/lib/mix-compatibility/compatibility-rules.ts` (21 новый `createKnowledgeCategoryRule`), `src/lib/flavor-knowledge/knowledge.test.ts` (+7 тестов), `docs/adr/README.md`;
- не изменено: Prisma-схема/миграции (чисто knowledge-layer правки, без данных в БД), сами 5 конфликтующих и 2 дублирующих записи `CATEGORY_RELATIONS` (не тронуты).

**Тесты:** Vitest **1066/1066 passed, 32 файла** (было 1059/32, +7: FLORAL+CREAMY стала `COMPLEMENTARY_CONTRAST`, regression-guard на 5 конфликтующих парах с их ОРИГИНАЛЬНЫМИ значениями, явная запись `CITRUS+SPICE`/`NEUTRAL` в реестре, `createKnowledgeCategoryRule` бросает исключение для неё, минимальный вес evidence во всей таблице, и 2 сквозных теста через `analyzeNoteCompatibility` — `FLORAL+DAIRY` и `COFFEE+BERRY`); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** review-список из 5 конфликтующих пар (`TEA+CITRUS`, `DAIRY+BERRY`, `TEA+SPICE`, `MINT+DAIRY`, `COFFEE+VANILLA`) — требует отдельного, осознанного решения по каждой паре в будущей задаче, не решается автоматически.

## 2026-07-24 — ADR-019 addendum: разбор review-списка (5 конфликтов + 2 дубля)

**Сделано:** по запросу пользователя проведён построчный разбор всех 5 конфликтующих пар и 2 точных дублей из review-списка ADR-019 (для каждого конфликта — что было в старой записи и её источник, что предлагают новые данные и их доказательность, рекомендация). После показа анализа пользователь принял решения по каждому пункту: `TEA+CITRUS` и `DAIRY+BERRY` — взять новое (обе — усиление `GOOD_MATCH→STRONG_MATCH` без разворота знака, направление совпадает со старой эвристикой); `TEA+SPICE` — взять новое (`GOOD_MATCH`, безусловно → `COMPLEMENTARY_CONTRAST`, условно); `MINT+DAIRY`(`MINT+CREAMY`) и `COFFEE+VANILLA` — оставить старое без изменений (оба случая — разворот знака при недостаточной новой доказательности: `MEDIUM` против правдоподобной старой эвристики и единственный источник — маркетинговый текст одного товара, соответственно). Из дублей: `FLORAL+BERRY` — не менять (дополнительно подкреплён отдельным `claim.relation.berry-floral` со ссылкой на `evidence.verified.core-pairs`, вес 0.9 — самый высокий во всей системе); `DAIRY+DESSERT`(`DESSERT+CREAMY`) — значение/тип не менять, но прикрепить новую evidence-ссылку для аудируемости.

Для прикрепления доп. evidence к `DESSERT+CREAMY` без изменения значения добавлен новый необязательный параметр `extraEvidenceIds` у хелпера `relation()` (по умолчанию пустой массив, ни один существующий вызов не затронут). Критически важно: для 3 пересмотренных пар (`TEA+CITRUS`, `DAIRY+BERRY`, `SPICE+TEA`) добавлены соответствующие вызовы `createKnowledgeCategoryRule` в `compatibility-rules.ts` — без этого шага изменение значения в `CATEGORY_RELATIONS` не повлияло бы на реальный scoring вообще (эти 3 записи существовали в файле ещё до ADR-019 как чисто справочные, никогда не были подключены к `NOTE_COMPATIBILITY_RULES`). Это не было явно запрошено пользователем, но сделано как необходимое следствие решения «взять новое», иначе изменение было бы инертным — отмечено в отчёте пользователю отдельно.

**Файлы:**
- изменено: `src/lib/flavor-knowledge/category-relations.ts` (3 пары переведены с `relation()` на `researched()` с новыми значениями, `DESSERT+CREAMY` получил `extraEvidenceIds`, комментарии `ADR-019 review resolution` у всех 7 разобранных записей), `src/lib/mix-compatibility/compatibility-rules.ts` (+3 `createKnowledgeCategoryRule`), `src/lib/flavor-knowledge/knowledge.test.ts` (тест 63 сужен до 2 оставленных пар, добавлены тесты 69–74), `docs/adr/ADR-019-aggregated-research-category-compatibility-rules.md` (Addendum с итоговой таблицей решений);
- не изменено: `MINT+CREAMY`, `VANILLA+COFFEE`, `FLORAL+BERRY` — значения и evidence не тронуты, только добавлены комментарии, фиксирующие, что пара была рассмотрена и осознанно оставлена как есть.

**Тесты:** Vitest **1072/1072 passed, 32 файла** (было 1066/32, +6: новые значения `TEA+CITRUS`/`DAIRY+BERRY`/`SPICE+TEA`, их evidence, неизменность `DESSERT+CREAMY`(с доп. evidence)/`FLORAL+BERRY`, фактическое срабатывание всех 3 пересмотренных правил через `analyzeNoteCompatibility`); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** ничего — review-список ADR-019 закрыт полностью (все 5 конфликтов и оба дубля разобраны и применены).

## 2026-07-24 — приоритизация правил совместимости: аналитика + ADR-020 (Step A применён, Step B — предложение)

**Сделано:** по запросу пользователя посчитана приоритизация поиска новых правил совместимости на основе двух уже существующих в проекте источников (без внешнего поиска): (1) частота 24 категорий среди 86 импортированных продуктов (`FRUIT`=38 — доминирует, `FLORAL`/`SMOKY`=1 — редчайшие); (2) реальные пары категорий из 37 `status=VERIFIED` миксов `Mixes_Internal`/`Mix_Components` исходного workbook, сопоставленные с `reaction_class`. Для сопоставления сырых названий компонентов с canonicalProductId изначально использована не та функция (`resolveCatalogTobaccoIdentity` — общий резолвер брендов/линеек, 0% совпадений), исправлено на `resolveIdentityWithDecisions` против `TOBACCO_IDENTITY_DECISION_REGISTRY` (91% компонентов разрешено — 104 из 114). Получено 292 пары-вхождения по 120 уникальным парам; важная находка — `reaction_class` ни разу не был `negative` во всей истории (только `very_positive`/`positive`/`mixed`/`unknown`), что является ограничением выборки, а не доказательством отсутствия плохих сочетаний.

По итогам аналитики пользователь запросил два шага. **Step A** (реализован): программный аудит всех 46 записей `CATEGORY_RELATIONS` против фактически подключённых пар нашёл **19** несвязанных non-NEUTRAL записей (не 5, как в примере) — подключено **17** через `createKnowledgeCategoryRule` в `compatibility-rules.ts` без изменения значений, включая единственную во всей системе запись `CONFLICT` (`CANDY+SMOKY`), которая до этого ни разу не влияла на scoring. **2 пары не подключены технически** — `TOBACCO+WOODY`/`TOBACCO+FRUIT`: `TOBACCO` существует только в словаре `flavor-knowledge`, но никогда не добавлялась в legacy Prisma-enum (не используется в registry), а `createKnowledgeCategoryRule` требует legacy-аргументы.

**Step B** (инфраструктура реализована, конкретные значения — предложение, не применено): заведён новый evidence type `VERIFIED_MIX_HISTORY` (3 уровня по размеру выборки n: HIGH n≥10/вес 0.75, MEDIUM n=5-9/вес 0.55, LOW n=3-4/вес 0.35) с явной оговоркой в комментариях про отсутствие `negative`-примеров как ограничение выборки. Пересчитан топ-15 непокрытых пар (n≥3) уже ПОСЛЕ Step A — список изменился (например `CITRUS+FRUIT`, `CREAMY+DESSERT`, `FRUIT+SPICE` выпали, так как теперь уже подключены). Предложена консервативная методология (никогда не `STRONG_MATCH`, никогда `RISKY`/`CONFLICT` из этой истории) и таблица для всех 15 пар — 12 предлагается подключить как `COMPLEMENTARY_CONTRAST`, 2 (`FRUIT+SOUR`, `FRUIT+NUT`) оставить `NEUTRAL` из-за заметной доли `mixed`. Таблица и методология оформлены в ADR-020, но реализация (создание 12 записей `CATEGORY_RELATIONS`/`createKnowledgeCategoryRule`) не выполнена — ждёт решения пользователя по порогам/конкретным парам.

**Файлы:**
- создано: `docs/adr/ADR-020-wiring-audit-and-verified-mix-history-evidence.md`;
- изменено: `src/lib/mix-compatibility/compatibility-rules.ts` (+17 `createKnowledgeCategoryRule`), `src/lib/flavor-knowledge/types.ts` (`VERIFIED_MIX_HISTORY`), `src/lib/flavor-knowledge/constants.ts` (`EVIDENCE_TYPES`), `src/lib/flavor-knowledge/evidence.ts` (3 новые evidence-записи), `src/lib/flavor-knowledge/knowledge.test.ts` (+5 тестов), `docs/adr/README.md`;
- не изменено: `src/lib/flavor-knowledge/category-relations.ts` (Step A не меняет значения — только wiring в отдельном файле; Step B ничего не добавляет, пока нет решения).

**Тесты:** Vitest **1076/1076 passed, 32 файла** (было 1072/32, +5: 3 из Step A через `analyzeNoteCompatibility` — `FRUIT+CITRUS`, `CANDY+SMOKY`, `MINT+CREAMY`, + подтверждение, что `TOBACCO+WOODY` технически не подключаем, + упорядоченность весов новых evidence-тиров); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** решение пользователя по таблице ADR-020 (методология/пороги, и/или конкретные значения по каждой из 15 пар) — до этого Step B не реализуется в коде.

## 2026-07-24 — ADR-020 Step B: применение одобренной методологии (13 из 15 пар подключены)

**Сделано:** пользователь одобрил предложенную в ADR-020 таблицу и пороги без изменений. Реализовано: добавлен helper `verifiedHistory()` в `category-relations.ts` (по аналогии с `researched()` из ADR-019, но evidenceIds указывают на `evidence.verified-mix-history.{high,medium,low}`); добавлены все 15 записей `CATEGORY_RELATIONS` с типами и confidence-тирами точно по одобренной таблице. 13 из 15 подключены через `createKnowledgeCategoryRule` (все — `COMPLEMENTARY_CONTRAST`, `0.25`; ни одна пара не набрала HIGH-confidence + `positiveRatio`≥85%, нужные для `GOOD_MATCH`, что и предсказывалось при формулировке методологии). `FRUIT+SOUR` и `FRUIT+NUT` записаны как `NEUTRAL` (заметная доля `mixed`) и не подключены — `createKnowledgeCategoryRule` бросает исключение для `NEUTRAL`, та же схема, что и `CITRUS+SPICE` в ADR-019.

**Файлы:**
- изменено: `src/lib/flavor-knowledge/category-relations.ts` (`verifiedHistory()` + 15 записей), `src/lib/mix-compatibility/compatibility-rules.ts` (+13 `createKnowledgeCategoryRule`), `src/lib/flavor-knowledge/knowledge.test.ts` (+4 теста: 80–83), `docs/adr/ADR-020-...md` (addendum о применении), `docs/adr/README.md`.

**Тесты:** Vitest **1081/1081 passed, 32 файла** (было 1076/32, +5); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** ничего по этой задаче — приоритизация из предыдущей сессии полностью закрыта (Step A и Step B реализованы).

## 2026-07-24 — диагностика 6 компонентов predictedQualityScore + ADR-021 (анализ, без реализации)

**Сделано:** по запросу пользователя прогнана диагностика всех 6 компонентов `predictedQualityScore` (compatibility/proportions/componentQuality/balance/risks/confirmations) на тех же 12 реальных парах, что использовались в диагностике ADR-018. `compatibility` (range 0.8) и `risks` (range 1.5) теперь показывают реальную, содержательную вариацию — прямое следствие Шагов A+B ADR-020 (пара `Earth Wildberry Mors + Pumpkin RAF` получила и максимум `compatibility`, и минимум `risks` одновременно — сработала RISKY-связь `category.coffee-berry`). `proportions` оказался плоским (8.0/8.0) исключительно из-за методики теста (все 12 пар прогонялись при фиксированном 50/50) — проверено отдельно на одной паре с разными процентами (50/50→80/20→30/70→95/5): функция реально реагирует, просто имеет плато у сбалансированных пропорций. `balance` узкий (0.4) по уже известной причине (нехватка числовых core-полей → `NEUTRAL_FALLBACK=5`). `confirmations` оказался **абсолютно плоским (5.0/5.0)** при любом проценте — не тестовый артефакт, а реальный дефект: `fromPrismaFlavor` никогда не устанавливает `proportionConfirmed`/`independentEvidenceCount`, оба по умолчанию `false`/`0`.

По запросу пользователя оформлена **ADR-021** (только анализ, без кода): найдено, что у этих полей 2 потребителя — `confirmations` (10% веса `predictedQualityScore`) и отдельно `calculateMixConfidence()` (`proportionCoverage`/`externalEvidenceCoverage`, формирует лейбл `predictionConfidence`). Посчитан конкретный эффект второго потребителя: **ни один микс, целиком состоящий из новых 86 продуктов, не может достичь лейбла «Высокая» (максимум 71.5 из нужных 75)** — исключительно из-за двух зафиксированных на месте полей. Разобрано, откуда реально взять данные: `proportionConfirmed` требует НОВОГО артефакта — `VerifiedMixRecipeRegistry` (37 конкретных рецептов, которые ADR-020 никогда не извлекал как отдельные записи, только агрегированную статистику по парам категорий) — не просто «подключения», а полноценной кураторской работы. `independentEvidenceCount` — либо новый сбор данных (независимые публикации одного рецепта, которых сегодня физически нет в модели данных workbook), либо переиспользование evidence реестра Product Flavor Profile Registry (данные уже есть, но создаёт задвоение сигнала с `profileReliabilityScore`, который сам сейчас — плоская константа 60 для всех 86 продуктов). Также найдена техническая неточность в формулировке задачи: подключать логику нужно не в `fromPrismaFlavor` (не имеет контекста «весь микс»), а на уровне вызывающего кода в `/api/mixes/route.ts`. Рекомендация — низкий-средний приоритет: узкий охват (только точные совпадения с 37 рецептами против комбинаторно огромного пространства пользовательских миксов) при нетривиальных трудозатратах.

**Файлы:**
- создано: `docs/adr/ADR-021-proportion-confirmation-and-independent-evidence-analysis.md`;
- изменено: `docs/adr/README.md`;
- не изменено: весь код проекта — по явному указанию пользователя это чисто аналитическая задача.

**Тесты:** не запускались — код не менялся.

**Осталось:** решение пользователя, стоит ли реализовывать `VerifiedMixRecipeRegistry` (и как поступить с `independentEvidenceCount`/`profileReliabilityScore`) — не начато, ждёт приоритизации.

## 2026-07-27 — ADR-022: перекалибровка весов `calculateMixConfidence` + пояснение в UI

**Сделано:** пользователь отклонил реализацию `VerifiedMixRecipeRegistry` сейчас (низкий приоритет, принято по ADR-021), но запросил минимальную правку презентации: лейбл «Высокая уверенность» структурно недостижим для любого микса в системе из-за двух зафиксированных на месте осей формулы `calculateMixConfidence` (`proportionCoverage`=70, `externalEvidenceCoverage`=0 для 100% миксов). Реализовано оба предложенных пользователем варианта одновременно: (A) веса формулы перевешены с `identityCoverage×0.4 + profileCoverage×0.35 + proportionCoverage×0.15 + externalEvidenceCoverage×0.1` на `identityCoverage×0.55 + profileCoverage×0.45` — оба поля по-прежнему считаются и возвращаются в отчёте, просто исключены из веса `score`; в коде явный комментарий, что это временная мера до реализации `VerifiedMixRecipeRegistry` (ADR-021), веса нужно будет пересмотреть при её появлении; (B) в `confidenceReasons` (`mix-result-presentation/index.ts`) добавлен постоянный поясняющий пункт о том, что подтверждение пропорций и внешние источники пока не отслеживаются и не влияют на оценку — лимит показываемых причин увеличен с 3 до 4, чтобы не вытеснять более специфичные пункты.

При подготовке диагностики обнаружена и задокументирована неточность собственного анализа ADR-021: `profileCoverage` — не плоская константа `60` для всех 86 продуктов, как предполагалось (это относится к `reliability()` в `from-prisma.ts`), а честное среднее `reliabilityScore` по всем 18 полям `FlavorProfile`, где `null`-поля тянут его вниз через `NEUTRAL_FALLBACK` — на реальных данных варьируется `21.3–60`. Прогнана диагностика до/после на 12 реальных парах (включая эталонную `Sarma Горная лаванда + Daily Hookah Сливки`): диапазон confidence-score расширился с `27.5–60.1` до `23.0–67.9`; «Высокая» не достигнута ни разу на этой выборке (актуальный потолок теперь честно зависит от `profileCoverage` конкретных компонентов, а не от искусственной константы) — 3 пары перешли `Предварительная→Средняя`, 1 пара (не найденная в каталоге, `identity=0`) слегка просела, так как раньше её тянули вверх те же зафиксированные константы.

**Файлы:**
- создано: `docs/adr/ADR-022-mix-confidence-weight-recalibration.md`;
- изменено: `src/lib/canonical-mix-scoring/calculate-mix-confidence.ts` (веса `score`), `src/lib/mix-result-presentation/index.ts` (новый пункт `confidenceReasons`, лимит `slice(0,4)`), `docs/adr/README.md`.

**Тесты:** Vitest **1081/1081 passed, 32 файла** (без изменений количества — только значения весов, тест на конкретный лейбл `"Подтверждённая"` не затронут, т.к. его ветка не доходит до порогов); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** ничего по этой задаче; коммит — по отдельному запросу пользователя (не запрошен явно в этом сообщении).

## 2026-07-27 — скрытие процента уверенности в UI + починка self-pairing бага в note-compatibility

**Сделано (часть 1, UI):** по запросу пользователя карточка «Надёжность прогноза» (`src/app/result/[id]/page.tsx`) больше не показывает числовой процент уверенности напрямую в разметке — вместо `{label} · {score}%` теперь бейдж только с тиром (`Предварительная`/`Средняя`/`Высокая`/`Подтверждённая`), с CSS-only tooltip (`:hover`/`:focus`/`:focus-within`, без JS) — при наведении/фокусе раскрывается уже существующий текстовый `confidence.summary`. Процент остался в данных (`presentation.confidence.score`), просто не рендерится. Добавлены классы `.confidence-badge`/`.confidence-tooltip(-content)` в `globals.css`, по цвету на тир.

**Сделано (часть 2, диагностика без кода):** по запросу пользователя разобран случай `Chabacco Medium Belgian Cider + Musthave Vanilla Cream`, показавший подозрительное `Category relation COMPLEMENTARY_CONTRAST` в «Сильных сторонах». Найдено: сработали `category.creamy-fruit` (COMPLEMENTARY_CONTRAST, реальная межпродуктовая пара DAIRY+FRUIT, не ALCOHOL+DAIRY) и `category.alcohol-fruit` (GOOD_MATCH, **самопара** — обе ноты, ALCOHOL и FRUIT, принадлежат ОДНОМУ товару, Cider; подтверждено экспериментально подстановкой несвязанного второго компонента — правило сработало всё равно). Причина — `analyzeNoteCompatibility` перебирает пары нот по всему пулу микса без проверки, что они пришли от разных компонентов; `MixProfileNoteResult` не хранил источник компонента на верхнем уровне. Дополнительно посчитано: 10 из 24 категорий (включая `ALCOHOL` и самую используемую `FRUIT`) не имеют вообще ни одного RISKY/CONFLICT-правила; `BEVERAGE` не имеет вообще никаких связей; и **63% (29 из 46)** подключённых позитивных правил и **57% (53 из 93)** товаров каталога подвержены той же self-pairing уязвимости.

**Сделано (часть 1 приоритета — починка бага):** в `MixProfileNoteResult` (`src/lib/mix-profile/types.ts`) добавлено поле верхнего уровня `componentFlavorIds` (различные `flavorId`, реально вносящие вклад в эту агрегированную ноту) — заполняется в `calculate-note-contributions.ts` из `sources[]`. В `analyzeNoteCompatibility` (`src/lib/mix-compatibility/analyze-note-compatibility.ts`) добавлено условие `fromDifferentComponents` — пара засчитывается, только если объединение `componentFlavorIds` обеих нот содержит ≥2 разных id (не считает самопару одного товара, но не блокирует случай, когда одна и та же категория подтверждена несколькими компонентами). Обновлены тестовые фикстуры, неявно полагавшиеся на прежнее поведение: `mix-profile.test.ts` (3 фейковых массива нот для `classifyNotes`, добавлен `componentFlavorIds:[i]`) и `knowledge.test.ts` (хелпер `profileNote` теперь выдаёт каждому вызову свой уникальный id счётчиком — все ~20 существующих вызовов и так задумывались как пары РАЗНЫХ товаров, поэтому логика тестов не менялась, только фикстура).

Диагностика до/после на тех же 12 парах + явно на Cider+Vanilla Cream (git stash/pop для честного before/after на одном и том же коде): `category.alcohol-fruit` у Cider+Vanilla Cream пропал (`predictedQualityScore` 7.7→7.6, `compatibility` 8.3→8.1), `category.creamy-fruit` остался как реальная межкомпонентная связь. Самый заметный эффект — `Sarma Классическая Банановое суфле + Sarma 360 Огуречный лимонад`: пропали сразу 3 самопарных правила (`dessert-creamy` STRONG_MATCH 0.7, `creamy-fruit`, `dessert-fruit` — все внутренние для «суфле»), реальные `fruit-citrus`/`fruit-fresh`/`citrus-creamy`/`citrus-dessert` остались. `Overdose Coffee + Element Earth Wildberry Mors`: `compatibility` просел сильнее всех (8.9→8.2) — у Overdose Coffee оказался собственный кластер самопар (`coffee-creamy`, `creamy-chocolate`, часть `dessert-creamy`). 6 из 13 пар не изменились вообще (`Element+Sebero`, `Sarma Персик+Daily Сливки`, `Apple+Jelly`, `Element+Джин`, `HIT+Сливочный крем`, `Sebero+Melon`) — подтверждает, что фикс хирургический и не трогает пары без самопар.

**Файлы:**
- изменено: `src/app/result/[id]/page.tsx`, `src/app/globals.css` (часть 1); `src/lib/mix-profile/types.ts`, `src/lib/mix-profile/calculate-note-contributions.ts`, `src/lib/mix-compatibility/analyze-note-compatibility.ts`, `src/lib/mix-profile/mix-profile.test.ts`, `src/lib/flavor-knowledge/knowledge.test.ts` (часть 1 приоритета);
- не создано: новая ADR — не запрошена явно для этой задачи (задокументировано здесь и в переписке); расширение risk-покрытия (10 непокрытых категорий) — явно отложено пользователем как отдельная задача (Приоритет 2).

**Тесты:** Vitest **1081/1081 passed, 32 файла** (без новых тестов — только правка 4 существующих фикстур под новое обязательное поле типа); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** Приоритет 2 (расширение RISKY/CONFLICT-покрытия для 10 непокрытых категорий, в т.ч. `ALCOHOL`/`FRUIT`/`BEVERAGE`) — отдельная будущая задача, сознательно не начата в этой сессии. Коммит — по отдельному запросу пользователя.

## 2026-07-28 — ADR-023: NEUTRAL_FALLBACK больше не подменяет значение поля числом

**Сделано:** по запросу пользователя реализован фикс диагностированной ранее (без кода) проблемы: `buildEffectiveTobaccoProfile` при отсутствии измерения подставлял число `5` прямо в значение поля (не только в `reliabilityScore`), из-за чего уже написанная под ADR-015/017 null-исключающая логика в `calculateWeightedProfile`/`analyzeProfileBalance`/`componentQuality()` никогда не получала настоящий `null` на живом пути. Теперь fallback передаёт `value: null` (только `reliabilityScore: 15` отмечает поле как неизмеренное); `EffectiveParameter.value`/`EffectiveTobaccoProfile.strengthLevel5`/`MixScoreBreakdown.componentQuality`/`balance` стали `number | null`.

Добавлен `weightedPartial()` в `calculate-canonical-mix-score.ts` — компонент без единого измеренного поля исключён из взвешенного среднего (не считается нулём); если ВСЕ компоненты микса ничего не измерили — `componentQuality`/`balance` (через новую `hasAnyProfileField`) становятся `null` целиком и **исключаются из `predictedQualityScore`** с перевзвешиванием остальных компонентов пропорционально (тот же приём, что ADR-022 уже применил к `calculateMixConfidence`; `confirmations` не тронут по явному указанию, участвует в перевзвешивании только как получатель освободившегося веса). Ложные позитивные факторы (`profile.sweet-sour-balance`/`fresh-juicy`/`dessert-balance`, срабатывавшие на паре из двух fallback-значений «5», ведь 5 — ровно "безопасная середина" большинства пороговых правил) устранены автоматически самой починкой null-пропагации — отдельного механизма не потребовалось, поскольку правила и так уже требовали `!== null`.

Диагностика до/после на тех же 13 парах (`git stash`/`pop` для честного сравнения): 5 из 13 пар потеряли хотя бы один ложный фактор (3 из них — сразу все 3 разом: Cider+Vanilla Cream, Sarma Лимонад+Daily Крем). `componentQuality` заметно вырос там, где раньше половина полей была fallback (Apple+Jelly 6.7→8.8, Лимонад+Крем 5.9→8.0). `Sarma Суфле+Лимонад` — единственная пара с `componentQuality=null` целиком (0 измеренных полей у обоих компонентов); `predictedQualityScore` при этом не упал, а вырос (7.6→7.9) — перевзвешивание убрало заниженный вклад несуществующих данных, а не наказало микс.

**Побочная находка (не исправлена, вне объёма):** диагностика пары `HIT Banana Shake + Daily Hookah Сливочный крем` после фикса упала с `Сумма должна быть 100%, получено 95` — обнаружен пред­существующий баг в `calculate-mix-analysis.ts` (`compareProposal`): `proposedInput` ищет процент по сырому `flavorId` вместо канонического id из `variant.componentId`, из-за чего для `RESOLVED`-товара с отличающимся каноническим id доля не подставляется. Баг не создан этой сессией — просто впервые задет новым (более честным) набором рекомендаций для этой конкретной пары. Не исправлялся — другой модуль, не заявлен в объёме ADR-023.

**Файлы:**
- создано: `docs/adr/ADR-023-neutral-fallback-null-propagation.md`;
- изменено: `src/lib/canonical-mix-scoring/build-effective-tobacco-profile.ts`, `src/lib/canonical-mix-scoring/calculate-canonical-mix-score.ts`, `src/lib/canonical-mix-scoring/types.ts`, `src/lib/canonical-mix-scoring/workbook-audit.ts`, `src/lib/mix-result-presentation/index.ts`, `src/app/result/[id]/page.tsx`, `src/lib/canonical-mix-scoring/canonical-mix-scoring.test.ts` (+2 теста), `src/lib/canonical-mix-scoring/calculate-canonical-mix-score.test.ts` (+3 теста), `src/lib/mix-result-presentation/mix-result-presentation.test.ts` (тип), `docs/adr/README.md`.

**Тесты:** Vitest **1086/1086 passed, 32 файла** (+5 новых); `tsc --noEmit` — чисто; `eslint .` — чисто.

**Осталось:** пред­существующий баг `compareProposal` (сопоставление по сырому `flavorId` вместо канонического id) — отдельная будущая задача, не начата. Коммит — по отдельному запросу пользователя.
