import type { FlavorNoteCategory, FlavorNoteType, FlavorProfile } from "../flavors/types";
import type { MixProfileComponentInput } from "./types";

export type PrismaFlavorForMixProfile=FlavorProfile&{id:number;name:string;slug:string;brand:{name:string};notes:Array<{intensity:number;noteType:FlavorNoteType;flavorNote:{id:number;name:string;slug:string;category:FlavorNoteCategory}}>};
export function fromPrismaFlavor(flavor:PrismaFlavorForMixProfile,percentage:number):MixProfileComponentInput{return{flavorId:flavor.id,brandName:flavor.brand.name,flavorName:flavor.name,flavorSlug:flavor.slug,percentage,profile:{sweetness:flavor.sweetness,acidity:flavor.acidity,bitterness:flavor.bitterness,creaminess:flavor.creaminess,cooling:flavor.cooling,strength:flavor.strength,intensity:flavor.intensity,heatResistance:flavor.heatResistance,dryness:flavor.dryness,juiciness:flavor.juiciness,freshness:flavor.freshness,dessertLevel:flavor.dessertLevel,spiceLevel:flavor.spiceLevel,floralLevel:flavor.floralLevel,herbalLevel:flavor.herbalLevel,smokyLevel:flavor.smokyLevel,naturalness:flavor.naturalness,persistence:flavor.persistence},notes:flavor.notes.map(assignment=>({noteId:assignment.flavorNote.id,noteName:assignment.flavorNote.name,noteSlug:assignment.flavorNote.slug,category:assignment.flavorNote.category,intensity:assignment.intensity,noteType:assignment.noteType}))}}
